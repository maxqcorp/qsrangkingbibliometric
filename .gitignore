# Manuscripts / documents — not pushed (per request)
*.docx
*.doc
*.pdf

# Code and intermediate data — not pushed (per request)
# Repo holds only the published outputs: supplementary file(s), figures/, tables/.
*.py
harmonized.*
summary.json

# Article templates and redundant archives — not pushed
*.dot
*.dotx
*.zip

# OS / Python cruft
.DS_Store
__pycache__/
*.pyc
.ipynb_checkpoints/
